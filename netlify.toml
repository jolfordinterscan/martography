[build]
  command = "npm run build"
  publish = "dist/client"

# TanStack Start's official Netlify Vite adapter generates the SSR function
# and its route handling. A generic SPA catch-all redirect is intentionally
# omitted because it would bypass server rendering and server routes.
